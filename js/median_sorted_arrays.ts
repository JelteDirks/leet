// start at a point where both arrays definitely need to be past to get the median
// then step using binary search to the correct point?
function findMedianSortedArrays(nums1: number[], nums2: number[]): number {

};
