// make adj list
// loop nodes
// for every node traverse in bfs and simultaneasly check the position of the
// strings at depth of the bfs at that time.
// if no strings have length of pos, continue to the next node and start the
// traversing there.
// add the found words to a list. can remove words to search for efficiency,
// no need to do this to improve running time.
